<h1>Edit Comment</h1>

<%= form_with(model: [article, comment], url: article_comment_path(article, comment), method: :patch) do |form| %>
  <p>
    <%= form.label :content %><br>
    <%= form.text_area :content %>
  </p>
  <p>
    <%= form.submit 'Update Comment' %>
  </p>
<% end %>